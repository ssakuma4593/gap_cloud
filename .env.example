# AWS Credentials for S3 Access
# You can obtain these from your AWS Console -> IAM -> Users -> Security Credentials
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here

# Optional: AWS Region (defaults to us-east-1 if not specified)
AWS_DEFAULT_REGION=us-east-1

# PostgreSQL Database Configuration for Amazon RDS
# Use AWS Secrets Manager or environment variables to securely store database credentials
# DO NOT commit real credentials to version control

# Option 1: Use AWS Secrets Manager (recommended for production)
# The application will automatically retrieve credentials from AWS Secrets Manager
# DATABASE_SECRET_NAME=research-gap-app/rds-credentials
# DATABASE_REGION=us-east-1

# Option 2: Environment variables (for development/testing only)
# DATABASE_URL=postgresql://username:password@your-rds-endpoint.region.rds.amazonaws.com:5432/research_gaps

# Option 3: Individual components (for development/testing only)
# DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
# DB_PORT=5432
# DB_NAME=research_gaps
# DB_USER=your_db_username
# DB_PASSWORD=your_db_password
